# stockNN

This repo contains data for training ML models to predict the stock market.

The `src` dir contains empty Python scripts that you can easily put your models into and train on the data provided in the `data` dir
